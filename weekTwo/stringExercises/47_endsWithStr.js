"use strict";
// Write a JavaScript function to test whether a string ends with a specified string.

const ends = (str, end) => str.endsWith(end);

console.log(ends("JS string exercises", "exercises"));
