"use strict";
// Write a JavaScript function to concatenate a given string n times (default is 1).

const repeatStr = (str, n) => str.repeat(n);

console.log(repeatStr("Ha!", 4));
console.log(repeatStr("Hola", 2));
