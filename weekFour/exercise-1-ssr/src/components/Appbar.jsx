import { AppBar, Toolbar, Typography } from "@mui/material";

const Appbar = () => {
  return (
    <div>
      <AppBar>
        <Toolbar>
          <Typography>Hello</Typography>
        </Toolbar>
      </AppBar>
    </div>
  );
};

export default Appbar;
